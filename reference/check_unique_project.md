# check whether project is unique or already on database

check whether project is unique or already on database

## Usage

``` r
check_unique_project(ProjectShortName, con)
```

## Arguments

- ProjectShortName:

  A list of pointblank agents (directly from camera_trap_dq())

- con:

  database connection

## Value

message
